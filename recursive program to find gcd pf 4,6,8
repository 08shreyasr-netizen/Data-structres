#include<stdio.h>
#include<conio.h>
int gcd(int m,int n )
{
  if(n==0)
  return(m)
  else
    if(n>m)
      return(gcd(n,m));
             else
               return(gcd(n,m,%n));
                      }
                      int main();
{
  int k,n,m;
  clrscr();
  printf(enter the number /n");
scanf("%d,%d,%d &k,&m,&n);
  printf("gcd(%d%d%d)=%d\n",k,m,n,gcd(k,gcd(m,n)));
  getch();
  }

